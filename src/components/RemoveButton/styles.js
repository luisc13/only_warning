import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 150,
    height: 150,
    borderRadius: 8,
    backgroundColor: '#fff',
    alignItems: 'center',
    flexDirection: 'row',
    marginLeft: 30,
    marginTop: 60,
  },
  icon:{
    width: 50,
    height: 50,
    marginLeft: 50
  }
});